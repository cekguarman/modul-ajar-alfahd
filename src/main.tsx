import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './alfahd' // <--- Sesuaikan dengan nama file .tsx aplikasi ini di folder src/
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
